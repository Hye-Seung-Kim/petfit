# Generated outputs

Real API images and their adjacent `*.json` metadata files are written here. No mock image is included because no approved source dog image was available.

For a demo fallback, store a prevalidated result here only after obtaining permission for its dog and outfit inputs. Label that UI path **Demo Mode**; never silently return this file after a real API failure.
